{
	"name": "devai-bridge",
	"description": "DEV-AI Suite bridge plugin for the embedded DSH runtime: serves bridge protocol v0 (JSON-RPC 2.0 over NDJSON stdio) so the IDE Agent Host can create, list, resume, and drive harness sessions, stream session events, and answer approval requests",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"main": "src/index.js",
	"peerDependencies": {
		"@deepseek-ai/dsh-llm": "^0.1.1-rc.2",
		"@deepseek-ai/dsh-sdk-protocol": "^0.1.1-rc.2",
		"@deepseek-ai/dsh-session": "^0.1.1-rc.2"
	}
}
