{
	"name": "dsh-plugin-approval-probe",
	"description": "Third-party-style dsh plugin used by the bridge contract test: registers one tool and gates it behind the approval seam, so a model tool call exercises the bridge's approval passthrough",
	"version": "1.0.0",
	"private": true,
	"type": "module",
	"main": "index.js",
	"peerDependencies": {
		"@deepseek-ai/dsh-tools": "^0.1.1-rc.2"
	}
}
